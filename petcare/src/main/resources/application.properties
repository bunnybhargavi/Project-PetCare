# Server Configuration
spring.application.name=petcare-backend
server.port=8080

# Database Configuration
spring.datasource.url=jdbc:mysql://localhost:3306/petcare_db
spring.datasource.username=root
spring.datasource.password=Bhargavi@0334
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# JPA Configuration
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQL8Dialect
spring.jpa.properties.hibernate.format_sql=true

# Flyway Configuration
spring.flyway.enabled=true
spring.flyway.baseline-on-migrate=true
spring.flyway.locations=classpath:db/migration

# JWT Configuration
jwt.secret=petcare-super-secret-jwt-key-2025-change-this-in-production
jwt.expiration=86400000

# Email Configuration (Gmail)
spring.mail.host=smtp.gmail.com
spring.mail.port=587
spring.mail.username=petcare.customerservices@gmail.com
spring.mail.password=gkufzulypvmhzulh
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true

# File Upload Configuration
spring.servlet.multipart.max-file-size=5MB
spring.servlet.multipart.max-request-size=5MB

# Upload Directory Configuration
app.upload.dir=uploads

# Logging
logging.level.com.pets.petcare=DEBUG
logging.file.name=application.log

# PayPal Configuration (Sandbox Mode)
# Using mock payment for testing - replace with real credentials for production
# To use real PayPal: Get credentials from https://developer.paypal.com/
paypal.client.id=your_client_id_here
paypal.client.secret=your_client_secret_here
paypal.mode=sandbox
paypal.webhook.id=WH-1234567890

# Note: With invalid credentials, the system will automatically use mock payments
# Mock payments will auto-approve and complete the order for testing purposes